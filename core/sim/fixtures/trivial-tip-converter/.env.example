# PORT — the dev/start server port. Read by `npm run dev` / `npm run start`
# (package.json: `next dev -p ${PORT:-3100}`) so concurrent replicas can each
# bind their own port via env instead of colliding on a hardcoded default.
PORT=3100
